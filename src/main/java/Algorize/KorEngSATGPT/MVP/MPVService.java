package Algorize.KorEngSATGPT.MVP;

public class MPVService {

    // make function getText
    /*
    make function getText
    input will be a image, this will be done by getting the url of the image
    using tesseract library, we will get the text
    if there is no text available, we will raise error "No text found"
    we will save the imageurl and the text string

    also make getAnswer
    input will be getting the text string from the DB
     */
}
